import LoginPage from '@/app/auth/login/page'

export default function LoginLayout(children){
    return (
        <html>
            <body>
                <LoginPage/>
            </body>
        </html>
    )
}